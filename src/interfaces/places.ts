export interface Place {
  title: string
  _id: string
  costumes: Array<String>
}

export interface Employee {
  fName: string
  lName: string
  numberPhone: string
  role: string
  costumes: string[]
}
